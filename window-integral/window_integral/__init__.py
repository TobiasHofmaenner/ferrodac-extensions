"""window-integral — a reference ferroDAC extension.

Importing the modules named in ``ferrodac-extension.toml`` is what registers their
providers with ferroDAC (there is no magic beyond a normal Python package).
"""
